dolmen.message
**************

``dolmen.message`` is a package in charge of providing a backbone for a
messaging system, revolving around 2 components: the receiver and the source.

It provides a prototyping for these 2 components, plus a message definition.
In addition, ``dolmen.message`` provides a default source implementation,
using the session, in order to persist messages.
